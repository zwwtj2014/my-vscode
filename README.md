# Clam VSCode README

**Some useful features for everyone.**

## Features

- [x] Open the markdown file with the specified application

  ```json
    // Set default app to open markdown
    "openMarkdown.defaultApp": "typora",

    // Set Identify the electron app
    "openMarkdown.isElectron": true
  ```
